╔════════════════════════════════════════════════════════════════╗
║             QUOTE GENERATOR API - QUICK COMMANDS               ║
╚════════════════════════════════════════════════════════════════╝
🐳 LOCAL TESTING (Docker)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
chmod +x start-docker.sh          # Make script executable
./start-docker.sh                 # Start everything (easiest!)
docker-compose up -d              # Start in background
docker-compose logs -f            # View logs
docker-compose down               # Stop services
curl http://localhost:8080/api/quotes/random  # Test API
🚂 RAILWAY.APP DEPLOYMENT (Recommended)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Go to: https://railway.app
2. Login with GitHub
3. New Project → Deploy from GitHub
4. Select repo: OnlineQuotesAPIs
5. Add → Database → MySQL
6. Deploy automatically!
🎨 RENDER.COM DEPLOYMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Go to: https://render.com
2. New → Web Service → Connect GitHub
3. Build: mvn clean package -DskipTests
4. Start: java -jar target/Quotes-0.0.1-SNAPSHOT.jar
5. New → PostgreSQL
6. Add env: DATABASE_URL
✈️  FLY.IO DEPLOYMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
curl -L https://fly.io/install.sh | sh
flyctl auth login
flyctl launch
flyctl deploy
🐛 TROUBLESHOOTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ss -ltnp | grep :8080             # Check port 8080
kill <PID>                        # Kill process
docker-compose logs mysql         # MySQL logs
docker-compose restart            # Restart services
mvn clean package -DskipTests     # Rebuild app
📖 DOCUMENTATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CHEAT_SHEET.md         → Quick reference (2 min)
QUICK_DEPLOY.md        → Fast deployment (5 min)
DEPLOYMENT_GUIDE.md    → Complete guide (30 min)
INDEX.md               → Navigation
FREE_DATABASE_OPTIONS.md → Databases
🎯 RECOMMENDED PATH
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. ./start-docker.sh               (test locally)
2. cat CHEAT_SHEET.md              (read quick guide)
3. Deploy to Railway.app           (go live!)
4. curl https://your-app.railway.app/api/quotes/random
╔════════════════════════════════════════════════════════════════╗
║  ✅ Everything is ready! Just run: ./start-docker.sh           ║
╚════════════════════════════════════════════════════════════════╝
